<h1><%= "Edit your Review" %></h1>

<% if flash.any? %>
  <div class="flash">
    <div class="container">
      <%flash[:notice].each do |f|%>
      <li>
        <%=f%>
      <%end%>
      </li>
    </div>
  </div>
<% end %>

<br />

<%= form_for [@country, @city, @review], method: :patch do |f| %>

<%= render :partial => "form_edit" %>

<% end %>
